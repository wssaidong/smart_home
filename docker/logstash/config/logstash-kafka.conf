input{
    kafka{
        group_id => "log-movie"
        topics => ["log-movie"]
        bootstrap_servers => "192.168.3.90:9092"
	codec => json {
		charset => "UTF-8"
        }
    }
}


output {
  elasticsearch{
    hosts => ["192.168.3.90:9200"]
    index => "movie-%{+YYYY.MM.dd}"
  }

  stdout{
    codec => rubydebug
  }
}